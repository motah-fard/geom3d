version: "2"

linters:
  default: standard
  enable:
    - unconvert
    - misspell
    - gocritic
    - unparam
  settings:
    gocritic:
      disabled-checks:
        - ifElseChain
        - singleCaseSwitch

formatters:
  enable:
    - gofmt
    - goimports
